# Point the frontend at the real backend API.
# When UNSET, the app runs against a built-in in-memory mock
# (seeded tables + demo accounts) so it works fully standalone.
#
# Set this to your deployed backend's base URL (including /api/v1)
# to use the live server instead.
VITE_API_URL=http://localhost:4000/api/v1
